services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: otplease
      POSTGRES_PASSWORD: otplease
      POSTGRES_DB: otplease
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
