# OTPlease dashboard

The operator console for OTPlease (Next.js, port 3000): applications, API keys, webhooks, deliveries, analytics, risk settings and the audit log. It signs in with the platform `ADMIN_TOKEN`.

```bash
npm run dev:dashboard
```

See the root README for the full stack.
